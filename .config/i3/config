# I3 config, an improved dynamic & tiling window manager.


# This file has been auto-generated by i3-config-wizard(1).
  # It will not be overwritten, so edit it as you like.
  # (https://i3wm.org/docs/userguide.html) for complete reference


# key basic
  #set $mod Mod4
  set $mod Mod1
  # Use Mouse+$mod to drag floating windows to their wanted position
  floating_modifier $mod

# key system
  #set $refresh_i3status killall -SIGUSR1 i3status
  set $refresh_i3blocks exec pkill -SIGRTMIN+11 i3blocks
  # key state, just send a singal
  bindsym --release Caps_Lock $refresh_i3blocks
  # print | screenshot
  bindsym --release Print exec --no-startup-id source ~/.config/i3/00/ky_print.sh && $refresh_i3blocks
  # mount | umount - udisk
  #bindcode 121 exec --no-startup-id source ~/.config/i3/00/ky_udisk.sh        && $refresh_i3blocks
  bindsym   F4  exec --no-startup-id source ~/.config/i3/00/ky_udisk.sh       && $refresh_i3blocks
  # change monitor
  bindsym   F3 exec --no-startup-id source ~/.config/i3/00/ky_monitor.sh      && $refresh_i3blocks
  # turn on | turn off - wlan | bluetooth
  bindsym   F7 exec --no-startup-id source ~/.config/i3/00/ky_bluetooth.sh    && $refresh_i3blocks
  bindsym   F8 exec --no-startup-id source ~/.config/i3/00/ky_wlan.sh         && $refresh_i3blocks
  # adjust volume by pactl in PulseAudio
  bindsym   F6 exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3blocks
  bindsym   F9 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5%  && $refresh_i3blocks
  bindsym  F10 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5%  && $refresh_i3blocks
  # adjust brightness by xbacklight with acpilight
  bindsym  F11 exec --no-startup-id xbacklight -dec 5                         && $refresh_i3blocks
  bindsym  F12 exec --no-startup-id xbacklight -inc 5                         && $refresh_i3blocks
  # Touchpad controls
  #bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad

# key exec app
  #bindsym $mod+Return exec alacritty
  bindsym $mod+d      exec dmenu_run
  bindsym $mod+t      exec alacritty
  bindsym $mod+b      exec firefox
  bindsym $mod+r      exec alacritty -e ranger
  bindsym $mod+v      exec alacritty -e vi
  #
  #bindsym F4 exec iw dev wlp1s0 scan | grep EEID
  #bindsym F3 exec wpa_supplicant -i wlp1s0 -c /home/oo/.config/wpa/wpa.conf

# key general
  # restart i3 inplace
  bindsym $mod+Shift+r restart
  # reload the configuration file
  bindsym $mod+Shift+c reload
  # exit i3 (logs you out of your X session)
  bindsym $mod+Shift+e exit i3
  #bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# key workspace
  # Define names for workspaces for which key bindings later on.
  # We use variables to avoid repeating the names in multiple places.
  set $ws1   "1"
  set $ws2   "2"
  set $ws3   "3"
  set $ws4   "4"
  set $ws5   "5"
  set $ws6   "6"
  set $ws7   "7"
  set $ws8   "8"
  set $ws9   "9"
  set $ws10 "10"
  # switch to workspace
  bindsym $mod+1 workspace number $ws1
  bindsym $mod+2 workspace number $ws2
  bindsym $mod+3 workspace number $ws3
  bindsym $mod+4 workspace number $ws4
  bindsym $mod+5 workspace number $ws5
  bindsym $mod+6 workspace number $ws6
  bindsym $mod+7 workspace number $ws7
  bindsym $mod+8 workspace number $ws8
  bindsym $mod+9 workspace number $ws9
  bindsym $mod+0 workspace number $ws10
  # move focused container to workspace
  bindsym $mod+Shift+1 move container to workspace number $ws1
  bindsym $mod+Shift+2 move container to workspace number $ws2
  bindsym $mod+Shift+3 move container to workspace number $ws3
  bindsym $mod+Shift+4 move container to workspace number $ws4
  bindsym $mod+Shift+5 move container to workspace number $ws5
  bindsym $mod+Shift+6 move container to workspace number $ws6
  bindsym $mod+Shift+7 move container to workspace number $ws7
  bindsym $mod+Shift+8 move container to workspace number $ws8
  bindsym $mod+Shift+9 move container to workspace number $ws9
  bindsym $mod+Shift+0 move container to workspace number $ws10

# key window
  # workspace focus
  bindsym Super_L              workspace next
  bindsym $mod+comma           workspace prev
  bindsym $mod+period          workspace next
  # window focus
  bindsym $mod+j               focus left
  bindsym $mod+k               focus down
  bindsym $mod+l               focus up
  bindsym $mod+semicolon       focus right
  bindsym $mod+w               focus next
  bindsym $mod+space           focus next
  # window ctrl
  bindsym $mod+q               kill
  bindsym $mod+x               layout toggle split
  bindsym $mod+Return          fullscreen toggle
  # window move
  bindsym $mod+Shift+j         move left
  bindsym $mod+Shift+k         move down
  bindsym $mod+Shift+l         move up
  bindsym $mod+Shift+semicolon move right
  # split orientation
  bindsym $mod+Shift+h         split h
  bindsym $mod+Shift+v         split v
  # #
    ## toggle
    #bindsym $mod+Shift+space     floating toggle
    #bindsym $mod+space           focus mode_toggle

    ## layout container (stacked, tabbed, toggle split)
    #bindsym $mod+s               layout stacking
    #bindsym $mod+w               layout tabbed

    ## focus container
    #bindsym $mod+a               focus parent
    #bindsym $mod+d               focus child

    ## alternatively, focus window
    #bindsym $mod+Left        focus left
    #bindsym $mod+Down        focus down
    #bindsym $mod+Up          focus up
    #bindsym $mod+Right       focus right

    ## alternatively, move window
    #bindsym $mod+Shift+Left  move left
    #bindsym $mod+Shift+Down  move down
    #bindsym $mod+Shift+Up    move up
    #bindsym $mod+Shift+Right move right

# key window resize
  # window move
  bindsym $mod+Up    move up
  bindsym $mod+Down  move down
  bindsym $mod+Left  move left
  bindsym $mod+Right move right
  # same bindings, but for the arrow keys
  bindsym $mod+Shift+Up    resize shrink height 5 px or 5 ppt
  bindsym $mod+Shift+Down  resize grow   height 5 px or 5 ppt
  bindsym $mod+Shift+Left  resize shrink width  5 px or 5 ppt
  bindsym $mod+Shift+Right resize grow   width  5 px or 5 ppt
  #bindsym $mod+shift+r mode "resize"
    #mode "resize" {
    # # These bindings trigger as soon as you enter the resize mode
    # bindsym j         resize shrink width  10 px or 10 ppt
    # bindsym k         resize grow   height 10 px or 10 ppt
    # bindsym l         resize shrink height 10 px or 10 ppt
    # bindsym semicolon resize grow   width  10 px or 10 ppt
    #
    # # same bindings, but for the arrow keys
    # bindsym Left      resize shrink width  10 px or 10 ppt
    # bindsym Down      resize grow   height 10 px or 10 ppt
    # bindsym Up        resize shrink height 10 px or 10 ppt
    # bindsym Right     resize grow   width  10 px or 10 ppt
    #
    # # back to normal: Enter or Escape or $mod+r
    # bindsym Return mode "default"
    # bindsym Escape mode "default"
    # bindsym $mod+r mode "default" }


# window style
  #
  default_orientation     horizontal
  workspace_layout        default
  default_border          pixel 1
  default_floating_border pixel 1
  popup_during_fullscreen smart
  #
  #new_window 1pixel
  gaps inner 4
  font pango:monospace 8

# window color
  client.background       #c0c0c0c0
  # class                 border    backgr.   text    indicator
  client.focused          #ffff0080 #ffff0080 #000000
  client.unfocused        #80808080 #80808080 #000000
  client.focused_inactive #80808080 #80808080 #000000
  client.urgent           #ff000080 #ff000080 #000000

# i3bar
  # (plus the system information i3status finds out, if available)
  bar {
  #status_command   while true; do echo "hello"; sleep 8; done
  #status_command   i3status -c ~/.config/i3/i3status.conf
  status_command   i3blocks -c ~/.config/i3/i3blocks.conf
  i3bar_command    i3bar    --transparency
  #tray_output none
  font pango:monospace bold 10
  separator_symbol "|"
  colors {
    background         #101010c0
    statusline         #ffffffff
    separator          #ffff00ff
    focused_statusline #ffffffff
    focused_background #101009c0
    focused_separator  #ffff00ff
    #                    #border     #bkgd       #text
    focused_workspace    #202020ff   #80808080   #ffffffff
    inactive_workspace   #202020ff   #202020c0   #ffffffff
    urgent_workspace     #202020ff   #ffff0080   #000000ff
    binding_mode         #202020ff   #202020c0   #ffffffff
    }}

# auto exec
  exec_always --no-startup-id source ~/.config/i3/00/autoexec.sh && pkill -SIGRTMIN+11 i3blocks
  #exec-always pkill -SIGRTMIN+11 i3blocks
  #exec_always sleep 3; xmodmap ~/.xmodmap
  #exec_always --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ 80% && $refresh_i3blocks
  #exec_always --no-startup-id xbacklight -set 80                       && $refresh_i3blocks
  #exec_always --no-startup-id picom -bcfCG --sw-opti
  #exec_always --no-startup-id feh --bg-fill --randomize /home/00/02_photo/*
  #exec_always --no-startup-id fcitx

# combination
  #
  # The combination of xss-lock, nm-applet and pactl is a popular choice,
  # so they are included here as an example. Modify as you see fit.

  # xss-lock grabs a logind suspend inhibit lock
  # and will use i3lock to lock the screen before suspend.
  # Use loginctl lock-session to lock your screen.
  #exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

  # NetworkManager is the pop way to manage wireless networks on Linux,
  # and nm-applet is a desktop environment-independent system tray GUI for it.
  #exec --no-startup-id nm-applet


